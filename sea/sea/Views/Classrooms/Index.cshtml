@model IEnumerable<sea.Classroom>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body><p>
    <a href="~/Admission_form/Index">Admission form</a>
    <a href="~/Students/Index">Student</a>
    <a href="~/Staffs/Index">Staff</a>
    <a href="~/Authorities/Index">Authorities</a>
    <a href="~/Classrooms/Index">Classroom</a>
    <a href="~/Maps/Index">Map</a>
    <a href="~/Libraries/Index">Library</a>
    <a href="~/Fees/Index">Fees</a>

</p><p>
    Child_Forms:
    <a href="~/Room_Type/Index">Room_Type</a></p>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Property1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class_No)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Room_Type.Name)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Property1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class_No)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room_Type.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
